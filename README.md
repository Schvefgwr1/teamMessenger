# TeamMessenger

TeamMessenger - это микросервисная платформа для командной работы, объединяющая чаты, управление задачами и систему уведомлений.

## 🏗️ Архитектура

Проект построен на микросервисной архитектуре с использованием следующих технологий:

### Backend
- **Язык программирования**: Go 1.23
- **База данных**: PostgreSQL
- **Кэш и сессии**: Redis
- **Файловое хранилище**: MinIO
- **Message Broker**: Apache Kafka
- **Email**: SMTP (Yandex)
- **API**: REST API с JWT аутентификацией
- **Gateway**: Nginx с rate limiting и WAF

### Frontend
- **React 18** + **TypeScript**
- **Vite** - сборщик проекта
- **TanStack Query** - серверное состояние
- **Zustand** - клиентское состояние
- **React Router v6** - маршрутизация
- **Tailwind CSS** - стилизация
- **Radix UI** - headless компоненты

## 📦 Микросервисы

### 🚪 API Service
**Точка входа в систему**
- API Gateway для всех запросов
- JWT аутентификация с динамической ротацией ключей
- Управление сессиями через Redis
- Кеширование горячих данных с Redis
- Маршрутизация к другим сервисам
- Автоматическая инвалидация сессий при смене ключей

### 👥 User Service
**Управление пользователями и безопасность**
- Регистрация и аутентификация пользователей
- Генерация и подпись JWT токенов
- Автоматическая ротация RSA ключей шифрования
- Отправка уведомлений о входе в систему
- Управление профилями пользователей

### 💬 Chat Service
**Система обмена сообщениями**
- Создание групповых и личных чатов
- Отправка и получение сообщений
- Поиск по сообщениям
- Уведомления о новых чатах
- Управление участниками чатов

### 📋 Task Service
**Управление задачами**
- Создание и назначение задач
- Отслеживание статусов задач
- Уведомления о новых задачах
- Фильтрация задач по пользователям
- Интеграция с системой уведомлений

### 📁 File Service
**Управление файлами**
- Загрузка файлов в MinIO
- Скачивание файлов
- Управление метаданными файлов
- Интеграция с другими сервисами

### 📧 Notification Service
**Система уведомлений**
- Обработка уведомлений через Kafka
- Отправка HTML email уведомлений
- Поддержка различных типов уведомлений:
  - Новые задачи
  - Новые чаты
  - Входы в систему
- Красивые HTML шаблоны

### 🎨 Frontend
**React SPA приложение**
- Пользовательский интерфейс
- Админ-панель
- Управление чатами, задачами и профилем
- Feature-Sliced Design архитектура

### 🔧 Common
**Общие компоненты**
- Модели данных
- Kafka producers/consumers
- Контракты сервисов
- Redis клиенты
- HTTP клиенты
- Конфигурация

## 🚀 Быстрый старт

### Предварительные требования

1. **Go 1.23+** (для локальной разработки backend)
2. **Node.js 18+** (для локальной разработки frontend)
3. **Docker** и **Docker Compose** (для запуска всей системы)
4. **PostgreSQL** (для локальной разработки)
5. **Redis** (для локальной разработки)
6. **Apache Kafka** (для локальной разработки)
7. **MinIO** (для локальной разработки)

### Запуск через Docker (рекомендуется)

Самый простой способ запустить всю систему:

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd teamMessenger

# 2. Настроить переменные окружения
cp compose.env.example compose.env
# Отредактируйте compose.env под ваши нужды

# 3. Запустить систему
make up
# или
docker compose -f docker-compose.yml --env-file compose.env up -d
```

После запуска система будет доступна:
- **Frontend**: http://localhost:8091
- **API Gateway**: http://localhost:8090/api/v1
- **Swagger документация**: http://localhost:8082/swagger/index.html (User Service)

Подробная документация по развертыванию: [DOCKER.md](./DOCKER.md)

### Локальная разработка

#### Backend

```bash
# Перейти в директорию сервиса
cd userService

# Установить зависимости
go mod download

# Настроить .env файл
cp env.example .env
# Отредактируйте .env

# Запустить сервис
go run cmd/server/main.go
```

#### Frontend

```bash
# Перейти в директорию frontend
cd frontend

# Установить зависимости
npm install

# Настроить переменные окружения
cp .env.example .env.local
# Отредактируйте .env.local

# Запустить dev server
npm run dev
```

Frontend будет доступен на http://localhost:3001

Подробная документация по frontend: [frontend/README.md](./frontend/README.md)

## 🔐 Система безопасности

### JWT с ротацией ключей
- UserService генерирует RSA ключи и автоматически обновляет их по расписанию
- Публичные ключи передаются в API Service через Kafka
- При смене ключей все активные сессии автоматически инвалидируются
- Обеспечивает высокий уровень безопасности

### Управление сессиями
- Сессии хранятся в Redis
- Автоматическое время жизни токенов
- Возможность принудительного выхода (logout)

### Nginx Gateway
- Rate limiting для защиты от DDoS
- WAF (Web Application Firewall) правила
- Blacklist/Whitelist IP адресов
- Проксирование запросов к микросервисам

## 🔄 Система уведомлений

### Kafka Integration
- Асинхронная обработка уведомлений
- Надежная доставка сообщений
- Масштабируемость

### Email уведомления
- HTML шаблоны для красивых уведомлений
- Поддержка SMTP (настроено для Yandex)
- Автоматическая отправка при:
  - Создании новых задач
  - Добавлении в чаты
  - Входе в систему

## 📊 Мониторинг и логирование

- Структурированные логи во всех сервисах
- Graceful shutdown для корректного завершения
- Обработка ошибок с детальной информацией
- Health check endpoints для всех сервисов

## 📚 Документация

- **[DOCKER.md](./DOCKER.md)** - Развертывание с Docker
- **[README-АРХИТЕКТУРА.md](./README-АРХИТЕКТУРА.md)** - Архитектура конфигурации
- **[frontend/README.md](./frontend/README.md)** - Документация Frontend
- **Swagger документация** - Доступна для каждого сервиса на `/swagger/index.html`

## 🧪 Тестирование

### Backend

```bash
# Unit тесты для всех сервисов
make test

# Полное тестирование (unit + integration)
make test-full

# Тесты конкретного сервиса
cd userService
go test ./...
```

### Frontend

```bash
cd frontend

# Запуск тестов
npm run test

# Тесты с покрытием
npm run test:coverage
```

## 🏗️ Структура проекта

```
teamMessenger/
├── apiService/          # API Gateway
├── userService/         # Управление пользователями
├── chatService/         # Чаты и сообщения
├── taskService/         # Управление задачами
├── fileService/         # Управление файлами
├── notificationService/ # Email уведомления
├── frontend/            # React SPA приложение
├── common/              # Общие компоненты
├── nginx/               # Nginx конфигурация
├── scripts/             # Вспомогательные скрипты
├── docker-compose.yml   # Docker Compose конфигурация
├── compose.env          # Переменные окружения для Docker
└── Makefile             # Команды для управления проектом
```

## 🔧 Управление проектом

### Основные команды Make

```bash
make help          # Показать все доступные команды
make up            # Запустить систему
make build-up      # Пересобрать и запустить
make down          # Остановить систему
make test          # Запустить тесты и систему
make test-full     # Полное тестирование
make debug         # Запуск в debug режиме
```

### Docker Compose команды

```bash
# Запуск
docker compose -f docker-compose.yml --env-file compose.env up -d

# Остановка
docker compose -f docker-compose.yml --env-file compose.env down

# Логи
docker compose logs -f

# Перезапуск сервиса
docker compose restart api-service
```

## 📝 Лицензия

Проект является частью Team Messenger системы.

---

**Версия**: 0.1.0  
**Последнее обновление**: 2024
