# Team Messenger Frontend

React SPA для корпоративного мессенджера с функционалом чатов, задач и администрирования.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Быстрый старт](#быстрый-старт)
- [Архитектура проекта](#архитектура-проекта)
- [Структура директорий](#структура-директорий)
- [Разработка](#разработка)
- [Тестирование](#тестирование)
- [Сборка и деплой](#сборка-и-деплой)
- [Environment Variables](#environment-variables)
- [API интеграция](#api-интеграция)
- [Документация](#документация)

## 🛠 Технологический стек

### Основные технологии

- **React 18** + **TypeScript** - UI библиотека и типизация
- **Vite 5** - быстрый сборщик и dev server
- **React Router v6** - маршрутизация
- **TanStack Query v5** - управление серверным состоянием
- **Zustand** - управление клиентским состоянием
- **Tailwind CSS** - utility-first CSS фреймворк
- **Radix UI** - headless UI компоненты
- **React Hook Form + Zod** - формы и валидация
- **Framer Motion** - анимации
- **Lucide React** - иконки
- **Axios** - HTTP клиент
- **Sonner** - toast уведомления
- **@dnd-kit** - drag-and-drop для Kanban доски

### Инструменты разработки

- **Vitest** - unit тестирование
- **Testing Library** - тестирование компонентов
- **ESLint** - линтинг кода
- **TypeScript** - статическая типизация
- **PostCSS** + **Autoprefixer** - обработка CSS

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** >= 18.x
- **npm** >= 9.x

### Установка зависимостей

```bash
npm install
```

### Разработка

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3001`

> **Примечание:** Порт по умолчанию - 3001 (настроен в `vite.config.ts`)

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в директории `dist/`

### Просмотр production сборки

```bash
npm run preview
```

## 📐 Архитектура проекта

Проект использует архитектуру **Feature-Sliced Design (FSD)** - методологию организации кода, которая разделяет приложение на слои и слайсы.

### Слои (снизу вверх)

1. **`shared`** - переиспользуемый код без бизнес-логики
   - UI компоненты (Button, Input, Modal и т.д.)
   - Утилиты (cn, formatDate, fileUrl)
   - Хуки (useDebounce, useLocalStorage)
   - API клиент
   - Типы и константы

2. **`entities`** - бизнес-сущности
   - `user` - пользователи
   - `chat` - чаты
   - `task` - задачи
   - `session` - сессия/авторизация

3. **`features`** - действия пользователя над сущностями
   - `auth/login` - вход в систему
   - `auth/register` - регистрация
   - `chat/create-chat` - создание чата
   - `profile/edit-profile` - редактирование профиля
   - `task/create-task` - создание задачи

4. **`widgets`** - композитные блоки UI
   - `Header` - шапка приложения
   - `Sidebar` - боковая панель навигации
   - `ChatList` - список чатов
   - `ChatWindow` - окно чата с сообщениями
   - `TaskBoard` - Kanban доска задач
   - `AdminPanel` - компоненты админ-панели
   - `layouts` - макеты страниц

5. **`pages`** - страницы приложения
   - `auth` - страницы авторизации
   - `dashboard` - главная страница
   - `chats` - страницы чатов
   - `tasks` - страницы задач
   - `profile` - профиль пользователя
   - `admin` - админ-панель
   - `errors` - страницы ошибок

6. **`app`** - инициализация приложения
   - Провайдеры (QueryProvider, AuthProvider)
   - Роутинг
   - Глобальные настройки

### Правила импортов

- Слой может импортировать только из слоёв **ниже** него
- Внутри слоя - только через публичное API (`index.ts`)
- ❌ Нельзя: `import { X } from '@/entities/user/model/store'`
- ✅ Можно: `import { useUser } from '@/entities/user'`

## 📁 Структура директорий

```
frontend/
├── .cursor/              # Правила для Cursor AI
├── public/               # Статические ресурсы
├── src/
│   ├── app/              # Инициализация приложения
│   │   ├── providers/    # React провайдеры
│   │   │   ├── AuthProvider.tsx
│   │   │   ├── QueryProvider.tsx
│   │   │   └── index.ts
│   │   ├── router/       # Роутинг и guards
│   │   │   ├── ProtectedRoute.tsx
│   │   │   ├── AdminRoute.tsx
│   │   │   ├── GuestRoute.tsx
│   │   │   └── index.ts
│   │   └── App.tsx       # Корневой компонент
│   │
│   ├── shared/           # Переиспользуемый код
│   │   ├── api/          # API клиент (Axios)
│   │   ├── ui/           # UI Kit компоненты
│   │   ├── hooks/        # Общие хуки
│   │   ├── lib/          # Утилиты
│   │   ├── constants/    # Константы
│   │   └── types/        # Общие типы
│   │
│   ├── entities/         # Бизнес-сущности
│   │   ├── user/         # Пользователи
│   │   ├── chat/         # Чаты
│   │   ├── task/          # Задачи
│   │   └── session/       # Сессия/авторизация
│   │
│   ├── features/         # Фичи (действия пользователя)
│   │   ├── auth/         # Авторизация
│   │   ├── chat/         # Действия с чатами
│   │   ├── profile/      # Действия с профилем
│   │   └── task/         # Действия с задачами
│   │
│   ├── widgets/          # Композитные блоки UI
│   │   ├── Header/        # Шапка приложения
│   │   ├── Sidebar/      # Боковая панель
│   │   ├── ChatList/     # Список чатов
│   │   ├── ChatWindow/   # Окно чата
│   │   ├── TaskBoard/    # Kanban доска
│   │   ├── AdminPanel/   # Компоненты админки
│   │   └── layouts/      # Макеты страниц
│   │
│   ├── pages/            # Страницы приложения
│   │   ├── auth/         # Авторизация
│   │   ├── dashboard/    # Главная
│   │   ├── chats/        # Чаты
│   │   ├── tasks/        # Задачи
│   │   ├── profile/      # Профиль
│   │   ├── admin/        # Админ-панель
│   │   └── errors/       # Страницы ошибок
│   │
│   ├── styles/           # Глобальные стили
│   │   ├── globals.css   # Tailwind + базовые стили
│   │   ├── variables.css # CSS переменные
│   │   └── media.css     # Медиа-запросы
│   │
│   ├── test/             # Настройки тестов
│   │   ├── setup.ts
│   │   └── test-utils.tsx
│   │
│   └── main.tsx          # Entry point
│
├── .env.example          # Пример env переменных
├── .gitignore
├── Dockerfile            # Production сборка
├── nginx.conf            # Nginx конфигурация
├── package.json
├── tsconfig.json         # TypeScript конфигурация
├── vite.config.ts        # Vite конфигурация
└── tailwind.config.js    # Tailwind конфигурация
```

## 💻 Разработка

### Доступные команды

```bash
# Разработка
npm run dev              # Запуск dev server

# Сборка
npm run build            # Production сборка
npm run preview          # Просмотр production сборки

# Качество кода
npm run lint             # Проверка линтером
npm run lint:fix         # Автоисправление ошибок линтера
npm run type-check       # Проверка типов TypeScript

# Тестирование
npm run test             # Запуск тестов в watch режиме
npm run test:ui          # Запуск тестов с UI
npm run test:run         # Запуск тестов один раз
npm run test:coverage    # Покрытие кода тестами
```

### Настройка IDE

Рекомендуется использовать **VS Code** с расширениями:

- **ESLint** - линтинг кода
- **Prettier** - форматирование
- **TypeScript** - поддержка TypeScript
- **Tailwind CSS IntelliSense** - автодополнение для Tailwind

### Path Aliases

Проект использует алиасы для импортов:

```typescript
// Вместо
import { Button } from '../../../shared/ui/Button';

// Используем
import { Button } from '@/shared/ui/Button';
```

Доступные алиасы:
- `@/*` → `src/*`

## 🧪 Тестирование

Проект использует **Vitest** для unit тестирования и **Testing Library** для тестирования компонентов.

### Структура тестов

Тесты находятся рядом с тестируемыми файлами:

```
src/
├── shared/
│   ├── lib/
│   │   ├── cn.ts
│   │   └── cn.test.ts      # Тест для cn
│   └── ui/
│       ├── Button/
│       │   ├── Button.tsx
│       │   └── Button.test.tsx  # Тест для Button
```

### Примеры тестов

```typescript
// src/shared/lib/cn.test.ts
import { describe, it, expect } from 'vitest';
import { cn } from './cn';

describe('cn', () => {
  it('should merge class names', () => {
    expect(cn('foo', 'bar')).toBe('foo bar');
  });
});
```

### Запуск тестов

```bash
# Watch режим (рекомендуется для разработки)
npm run test

# Один раз
npm run test:run

# С покрытием
npm run test:coverage
```

## 🚢 Сборка и деплой

### Production сборка

```bash
npm run build
```

Результат сборки будет в директории `dist/`:
- Оптимизированные JS/CSS файлы
- Code splitting (vendor, ui, query chunks)
- Source maps (только в development)

### Docker

Проект включает `Dockerfile` для production деплоя:

```bash
# Сборка образа
docker build -t team-messenger-frontend .

# Запуск контейнера
docker run -p 3000:80 team-messenger-frontend
```

### Nginx

В production используется Nginx для раздачи статических файлов. Конфигурация находится в `nginx.conf`.

Особенности:
- SPA fallback (все запросы на `index.html`)
- Gzip compression
- Кеширование статических ресурсов
- Security headers

## 🔧 Environment Variables

Создайте файл `.env.local` на основе `.env.example`:

```bash
# API URL
# Для разработки: http://localhost:8090
# Для production: /api (относительный путь через nginx)
VITE_API_URL=http://localhost:8090

# Feature flags
VITE_ENABLE_DEBUG=false
VITE_ENABLE_MOCK_API=false
```

### Доступные переменные

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `VITE_API_URL` | URL бэкенд API | `http://localhost:8090` |
| `VITE_ENABLE_DEBUG` | Включить debug режим | `false` |
| `VITE_ENABLE_MOCK_API` | Использовать mock API | `false` |

> **Важно:** Все переменные окружения должны начинаться с префикса `VITE_` для работы с Vite.

## 🔌 API интеграция

### API клиент

API клиент настроен в `src/shared/api/client.ts`:

- Базовый URL настраивается через `VITE_API_URL`
- Автоматическое добавление токена авторизации
- Обработка ошибок (401 → logout, 403 → forbidden)
- Поддержка CORS с credentials

### Структура API запросов

```typescript
// Пример использования
import { chatApi } from '@/entities/chat/api/chatApi';

// GET запрос
const chats = await chatApi.getUserChats(userId);

// POST запрос с FormData
const formData = new FormData();
formData.append('name', 'New Chat');
const chat = await chatApi.createChat(formData);
```

### React Query Hooks

Для работы с серверным состоянием используются React Query hooks:

```typescript
// src/entities/chat/api/queries.ts
import { useQuery, useMutation } from '@tanstack/react-query';
import { chatApi } from './chatApi';

// Query для получения данных
export const useUserChats = (userId: string) => {
  return useQuery({
    queryKey: ['chats', userId],
    queryFn: () => chatApi.getUserChats(userId),
  });
};

// Mutation для изменения данных
export const useCreateChat = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: (data: FormData) => chatApi.createChat(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['chats'] });
    },
  });
};
```

## 📚 Документация

Подробная документация находится в `.cursor/rules/`:

- **`00-implementation-roadmap.mdc`** - план реализации проекта
- **`01-project-structure.mdc`** - детальная структура проекта
- **`02-pages-and-routing.mdc`** - страницы и маршрутизация
- **`03-api-integration.mdc`** - интеграция с API
- **`04-ui-kit.mdc`** - UI компоненты и их использование
- **`05-state-management.mdc`** - управление состоянием
- **`06-admin-and-user-panels.mdc`** - пользовательский и админ кабинеты
- **`07-docker-deployment.mdc`** - Docker и деплой
- **`08-coding-conventions.mdc`** - конвенции кодирования

## 🎨 UI Kit

Проект включает набор переиспользуемых UI компонентов:

- **Button** - кнопки с вариантами (primary, secondary, outline, ghost, danger)
- **Input** - поля ввода с поддержкой иконок и ошибок
- **Card** - карточки для контента
- **Avatar** - аватары пользователей с fallback
- **Badge** - бейджи для статусов
- **Modal** - модальные окна
- **Dropdown** - выпадающие меню
- **Toast** - уведомления
- **Spinner** - индикаторы загрузки
- **Skeleton** - скелетоны для loading states
- **EmptyState** - пустые состояния

Все компоненты находятся в `src/shared/ui/` и экспортируются через barrel exports.

## 🔐 Авторизация

Система авторизации использует:

- **Zustand** для хранения токена и данных пользователя
- **ProtectedRoute** для защиты маршрутов
- **AdminRoute** для административных маршрутов
- **GuestRoute** для маршрутов только для неавторизованных

Токен хранится в `localStorage` и автоматически добавляется в заголовки API запросов.

## 📱 Маршруты

### Публичные маршруты

- `/login` - страница входа
- `/register` - страница регистрации

### Защищённые маршруты

- `/` - главная страница (Dashboard)
- `/profile` - профиль пользователя
- `/chats` - список чатов
- `/chats/:chatId` - конкретный чат
- `/tasks` - список задач (Kanban доска)
- `/tasks/:taskId` - детальная страница задачи

### Административные маршруты

- `/admin` - панель администратора
- `/admin/roles` - управление ролями пользователей
- `/admin/permissions` - просмотр разрешений
- `/admin/chat-roles` - управление ролями чатов
- `/admin/chat-permissions` - управление разрешениями чатов
- `/admin/task-statuses` - управление статусами задач
- `/admin/change-user-role` - изменение роли пользователя

### Страницы ошибок

- `/403` - доступ запрещён
- `*` - 404 страница не найдена

## 🤝 Вклад в проект

При разработке следуйте конвенциям:

1. **Именование файлов**: PascalCase для компонентов, camelCase для утилит
2. **Компоненты**: функциональные компоненты с TypeScript
3. **Импорты**: используйте path aliases (`@/`)
4. **Стили**: Tailwind CSS utility classes
5. **Типизация**: строгая типизация, избегайте `any`
6. **Тесты**: пишите тесты для утилит и критичных компонентов

Подробнее в `.cursor/rules/08-coding-conventions.mdc`

## 📄 Лицензия

Проект является частью Team Messenger системы.

---

**Версия:** 0.1.0  
**Последнее обновление:** 2024
